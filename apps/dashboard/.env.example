# Supabase — copy to .env.local and fill in real values from your project settings.
# Never commit real values: example files are tracked in git.
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
